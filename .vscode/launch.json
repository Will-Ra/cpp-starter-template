{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Active Target (自动识别)",
            "type": "lldb",
            "request": "launch",
            // 核心魔法：读取当前 CMake 选中的启动目标路径
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            // 核心魔法：读取当前目标所在的目录，或者直接用 workspaceFolder
            "cwd": "${workspaceFolder}"
        }
    ]
}